<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/style.css" />
    <title>show all user data</title>
 
</head>
<body class="bg-gray-100">

  <div class="flex">
    <form method="GET" action="/user/new">
        <button class="w-auto bg-indigo-600 m-2  hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
            Add New User
        </button>
        
      </form>
      <form method="GET" action="/">
        <button class="w-auto bg-indigo-600 m-2  hover:bg-indigo-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline">
         Total Number of user
        </button>
        
      </form>
    </div>

    <table class="min-w-full bg-white shadow-md border-collapse">
        <thead>
            <tr class="bg-red-200">
                <th class="border border-gray-300 px-4 py-2">Id</th>
                <th class="border border-gray-300 px-4 py-2">Email</th>
                <th class="border border-gray-300 px-4 py-2">User</th>
                <th class="border border-gray-300 px-4 py-2">Edit</th>
                <th class="border border-gray-300 px-4 py-2">Delete</th>
               
            </tr>
        </thead>
        <tbody>
            <% for(data of alldata) { %>
            <tr class="border border-gray-300">
                <td class="border border-gray-300 px-4 py-2"><%= data.id %></td>
                <td class="border border-gray-300 px-4 py-2"><%= data.email %></td>
                <td class="border border-gray-300 px-4 py-2"><%= data.username %></td>


                <td>
                    <form method="GET" action="/user/<%= data.id %>/edit" class="border  px-4 py-2">
                        <button >Edit username</button>
                    </form>
                </td>
                <td>
                    <form method="get" action="/user/<%= data.id %>/delete"class="border  px-4 py-2">
                        <button>Delete</button>
                    </form>
                </td>
            </tr>
            <% } %>
        </tbody>
    </table>

</body>
</html>